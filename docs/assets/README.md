# RepoPulse Assets

Store reviewed release screenshots and diagrams here.

The V1.0 README intentionally uses HTML comments instead of broken image links until real screenshots are captured from the running application.
